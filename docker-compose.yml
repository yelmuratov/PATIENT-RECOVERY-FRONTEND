version: "3.8"

services:
  patient-recovery-frontend:
    image: patient-recovery-frontend:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    restart: always
